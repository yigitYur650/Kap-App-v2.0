import 'package:flutter/material.dart';

/// Design system color tokens for Light and Dark modes.
class AppColors {
  // Light Theme structural colors
  static const lightBackground = Color(0xFFF5F5F3);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFEFEFED);

  // Dark Theme structural colors
  static const darkBackground = Color(0xFF0D0D0D);
  static const darkSurface = Color(0xFF1A1A1A);
  static const darkSurfaceVariant = Color(0xFF242424);

  // Accent palette - Light
  static const lightCoral = Color(0xFFFF6B6B);
  static const lightTeal = Color(0xFF0D9488);
  static const lightAmber = Color(0xFFD97706);
  static const lightPurple = Color(0xFF7C3AED);
  static const lightGreen = Color(0xFF059669);
  static const lightBlue = Color(0xFF2563EB);

  // Accent palette - Dark
  static const darkCoral = Color(0xFFFF8E8E);
  static const darkTeal = Color(0xFF2DD4BF);
  static const darkAmber = Color(0xFFFBBF24);
  static const darkPurple = Color(0xFFA78BFA);
  static const darkGreen = Color(0xFF34D399);
  static const darkBlue = Color(0xFF60A5FA);

  // Semantic - Light
  static const lightSuccess = Color(0xFF16A34A);
  static const lightWarning = Color(0xFFCA8A04);
  static const lightError = Color(0xFFDC2626);
  static const lightInfo = Color(0xFF2563EB);

  // Semantic - Dark
  static const darkSuccess = Color(0xFF4ADE80);
  static const darkWarning = Color(0xFFFACC15);
  static const darkError = Color(0xFFF87171);
  static const darkInfo = Color(0xFF60A5FA);
}
